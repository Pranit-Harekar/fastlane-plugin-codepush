describe Fastlane::Actions::CodepushAction do
  describe '#run' do
    it 'prints a message' do
      # expect(Fastlane::UI).to receive(:message).with("The codepush plugin is working!")

      # Fastlane::Actions::CodepushAction.run(nil)
    end
  end
end
